import { redis } from "../lib/redis.js";
import Product from "../models/product.model.js";
import cloudinary from "../lib/cloudinary.js";

export const getAllProducts = async (_req, res) => { try { res.json({ products: await Product.find({}).sort({ createdAt: -1 }) }); } catch (error) { res.status(500).json({ message: "Server error", error: error.message }); } };
export const getFeaturedProducts = async (_req, res) => { try { let products = await redis.get("featured_products"); if (products) return res.json(JSON.parse(products)); products = await Product.find({ isFeatured: true }).lean(); await redis.set("featured_products", JSON.stringify(products)); res.json(products); } catch (error) { res.status(500).json({ message: "Server error", error: error.message }); } };
export const createProduct = async (req, res) => { try { const { name, description, price, image, category, stock, lowStockThreshold, variants } = req.body; const normalizedStock = stock === undefined ? 100 : Number(stock); const threshold = lowStockThreshold === undefined ? 5 : Number(lowStockThreshold); if (!Number.isInteger(normalizedStock) || normalizedStock < 0) return res.status(400).json({ message: "Stock must be a non-negative integer" }); if (!Number.isInteger(threshold) || threshold < 0) return res.status(400).json({ message: "Low-stock threshold must be a non-negative integer" }); let cloudinaryResponse = null; if (image) cloudinaryResponse = await cloudinary.uploader.upload(image, { folder: "products" }); const product = await Product.create({ name, description, price, stock: normalizedStock, lowStockThreshold: threshold, variants: Array.isArray(variants) ? variants : [], image: cloudinaryResponse?.secure_url || "", category }); res.status(201).json(product); } catch (error) { res.status(500).json({ message: "Server error", error: error.message }); } };
export const updateProduct = async (req, res) => { try { const { name, description, price, category, image, stock, lowStockThreshold, variants } = req.body; const prod = await Product.findById(req.params.id); if (!prod) return res.status(404).json({ message: "Product not found" }); if (image && image !== prod.image) { const uploadRes = await cloudinary.uploader.upload(image, { folder: "products" }); if (prod.image) { const publicId = prod.image.split("/").pop().split(".")[0]; try { await cloudinary.uploader.destroy(`products/${publicId}`); } catch {} } prod.image = uploadRes.secure_url; } if (name !== undefined) prod.name = name; if (description !== undefined) prod.description = description; if (price !== undefined) prod.price = price; if (category !== undefined) prod.category = category; if (stock !== undefined) { const value = Number(stock); if (!Number.isInteger(value) || value < 0) return res.status(400).json({ message: "Stock must be a non-negative integer" }); prod.stock = value; } if (lowStockThreshold !== undefined) { const value = Number(lowStockThreshold); if (!Number.isInteger(value) || value < 0) return res.status(400).json({ message: "Low-stock threshold must be a non-negative integer" }); prod.lowStockThreshold = value; } if (variants !== undefined) { if (!Array.isArray(variants)) return res.status(400).json({ message: "Variants must be an array" }); prod.variants = variants; } await prod.save(); await updateFeaturedProductsCache(); res.json({ product: prod }); } catch (error) { res.status(500).json({ message: "Server error", error: error.message }); } };
export const getProductById = async (req, res) => { try { const product = await Product.findById(req.params.id); if (!product) return res.status(404).json({ message: "Product not found" }); res.json(product); } catch (error) { res.status(500).json({ message: "Server error", error: error.message }); } };
export const deleteProduct = async (req, res) => { try { const product = await Product.findById(req.params.id); if (!product) return res.status(404).json({ message: "Product not found" }); if (product.image) { const publicId = product.image.split("/").pop().split(".")[0]; try { await cloudinary.uploader.destroy(`products/${publicId}`); } catch {} } await Product.findByIdAndDelete(req.params.id); await updateFeaturedProductsCache(); res.json({ message: "Product deleted successfully" }); } catch (error) { res.status(500).json({ message: "Server error", error: error.message }); } };
export const getRecommendedProducts = async (_req, res) => { try { res.json(await Product.aggregate([{ $sample: { size: 4 } }, { $project: { _id: 1, name: 1, description: 1, price: 1, image: 1, stock: 1, lowStockThreshold: 1 } }])); } catch (error) { res.status(500).json({ message: "Server error", error: error.message }); } };
export const getProductsByCategory = async (req, res) => { try { res.json({ products: await Product.find({ category: req.params.category }) }); } catch (error) { res.status(500).json({ message: "Server error", error: error.message }); } };
export const toggleFeaturedProduct = async (req, res) => { try { const product = await Product.findById(req.params.id); if (!product) return res.status(404).json({ message: "Product not found" }); product.isFeatured = !product.isFeatured; const updated = await product.save(); await updateFeaturedProductsCache(); res.json(updated); } catch (error) { res.status(500).json({ message: "Server error", error: error.message }); } };
async function updateFeaturedProductsCache() { try { await redis.set("featured_products", JSON.stringify(await Product.find({ isFeatured: true }).lean())); } catch {} }
