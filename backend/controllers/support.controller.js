import crypto from "crypto";
import Subscriber from "../models/subscriber.model.js";
import ContactMessage from "../models/contact.model.js";
import { sendEmailSafely } from "../lib/email.js";
const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
const tokenHash = (token) => crypto.createHash("sha256").update(token).digest("hex");
const publicUrl = (path) => `${(process.env.PUBLIC_APP_URL || "http://localhost:5173").replace(/\/$/, "")}${path}`;
const issueUnsubscribeToken = () => crypto.randomBytes(32).toString("hex");
const escapeHtml = (value) => String(value).replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[char]));
export const subscribe = async (req, res) => { try { const email = String(req.body?.email || "").trim().toLowerCase(); if (!emailPattern.test(email) || email.length > 254) return res.status(400).json({ message: "Please provide a valid email address" }); let subscriber = await Subscriber.findOne({ email }); if (subscriber?.status === "subscribed") return res.status(200).json({ message: "You're already subscribed" }); const token = issueUnsubscribeToken(); const isNew = !subscriber; if (subscriber) { subscriber.status = "subscribed"; subscriber.subscribedAt = new Date(); subscriber.unsubscribedAt = undefined; subscriber.unsubscribeTokenHash = tokenHash(token); await subscriber.save(); } else await Subscriber.create({ email, unsubscribeTokenHash: tokenHash(token) }); await sendEmailSafely({ to: email, subject: "Welcome to Thread Theory", html: `<p>Thanks for subscribing to Thread Theory.</p><p>We’ll send occasional updates about new drops and offers.</p><p><a href="${publicUrl(`/unsubscribe?token=${encodeURIComponent(token)}`)}">Unsubscribe</a></p>` }, { email }); return res.status(isNew ? 201 : 200).json({ message: isNew ? "Thanks for subscribing!" : "Welcome back! You're subscribed" }); } catch (error) { if (error.code === 11000) return res.status(200).json({ message: "You're already subscribed" }); return res.status(500).json({ message: "Failed to subscribe" }); } };
export const unsubscribe = async (req, res) => { try { const token = String(req.query?.token || ""); if (!token || token.length > 200) return res.status(400).json({ message: "Invalid unsubscribe link" }); const subscriber = await Subscriber.findOne({ unsubscribeTokenHash: tokenHash(token) }); if (!subscriber) return res.status(404).json({ message: "This unsubscribe link is invalid or expired" }); subscriber.status = "unsubscribed"; subscriber.unsubscribedAt = new Date(); await subscriber.save(); return res.json({ message: "You have been unsubscribed" }); } catch { return res.status(500).json({ message: "Failed to unsubscribe" }); } };
export const getSubscribers = async (req, res) => { try { const status = req.query?.status; const search = String(req.query?.search || "").trim().slice(0, 100); const filter = {}; if (["subscribed", "unsubscribed"].includes(status)) filter.status = status; if (search) filter.email = { $regex: escapeHtml(search), $options: "i" }; return res.json({ subscribers: await Subscriber.find(filter).select("email status subscribedAt unsubscribedAt createdAt").sort({ createdAt: -1 }).lean() }); } catch { return res.status(500).json({ message: "Failed to fetch subscribers" }); } };
export const updateSubscriberStatus = async (req, res) => { try { if (!["subscribed", "unsubscribed"].includes(req.body?.status)) return res.status(400).json({ message: "Invalid subscriber status" }); const subscriber = await Subscriber.findById(req.params.id); if (!subscriber) return res.status(404).json({ message: "Subscriber not found" }); subscriber.status = req.body.status; if (subscriber.status === "subscribed") { subscriber.subscribedAt = new Date(); subscriber.unsubscribedAt = undefined; } else subscriber.unsubscribedAt = new Date(); await subscriber.save(); return res.json({ subscriber }); } catch { return res.status(500).json({ message: "Failed to update subscriber" }); } };
export const createContactMessage = async (req, res) => { try { const { name, email, subject, message } = req.body || {}; const normalizedEmail = String(email || "").trim().toLowerCase(); const cleanName = String(name || "").trim(), cleanSubject = String(subject || "").trim(), cleanMessage = String(message || "").trim(); if (!cleanName || !emailPattern.test(normalizedEmail) || !cleanSubject || !cleanMessage) return res.status(400).json({ message: "Name, email, subject, and message are required" }); if (cleanName.length > 100 || normalizedEmail.length > 254 || cleanSubject.length > 150 || cleanMessage.length > 5000) return res.status(400).json({ message: "One or more fields exceed the allowed length" }); const contactMessage = await ContactMessage.create({ name: cleanName, email: normalizedEmail, subject: cleanSubject, message: cleanMessage }); if (process.env.SUPPORT_EMAIL) await sendEmailSafely({ to: process.env.SUPPORT_EMAIL, subject: `New Thread Theory contact: ${escapeHtml(cleanSubject)}`, html: `<p><strong>From:</strong> ${escapeHtml(cleanName)} (${escapeHtml(normalizedEmail)})</p><p><strong>Subject:</strong> ${escapeHtml(cleanSubject)}</p><p>${escapeHtml(cleanMessage).replace(/\n/g, "<br>")}</p>` }, { type: "contact", id: String(contactMessage._id) }); return res.status(201).json({ message: "Your message has been sent", id: contactMessage._id }); } catch { return res.status(500).json({ message: "Failed to send message" }); } };
export const getContactMessages = async (_req, res) => { try { return res.status(200).json({ messages: await ContactMessage.find().sort({ createdAt: -1 }) }); } catch { return res.status(500).json({ message: "Failed to fetch contact messages" }); } };
export const updateContactStatus = async (req, res) => { try { const allowedStatuses = ["new", "in_progress", "resolved"]; if (!allowedStatuses.includes(req.body?.status)) return res.status(400).json({ message: "Invalid contact status" }); const message = await ContactMessage.findByIdAndUpdate(req.params.id, { status: req.body.status }, { new: true, runValidators: true }); if (!message) return res.status(404).json({ message: "Message not found" }); return res.status(200).json({ message }); } catch { return res.status(500).json({ message: "Failed to update message" }); } };
